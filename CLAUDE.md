# ucode admin frontend

Переписанная с нуля админка ucode. Старая версия живёт отдельно и сюда
не копируется.

## Читать перед задачей

1. [CONTEXT.md](CONTEXT.md) — словарь домена. Обязательно.
2. [docs/PLAN.md](docs/PLAN.md) — принятые решения и порядок работ.
3. [docs/adr/](docs/adr/) — решения, у которых есть цена.

## Старый репозиторий

`/Users/nurmuhammad/Documents/workspace/IT/udevs/ucode-frontend/ucode_admin_frontend`

**Это спецификация поведения, а не исходник.** Оттуда переносится ответ
на вопрос «что»: что делает фича, какие эндпоинты дёргает, какие поля
в ответе, какие крайние случаи покрыты.

Никогда не переносится «как»: код, структура файлов, имена. В том репозитории
290k строк, четыре поколения формы, три реализации таблицы, 359 ветвлений
по legacy-флагам и живые баги. Он не эталон.

### Где смотреть поведение

| Фича | Референс |
|---|---|
| View и его настройки | `src/views/views/` |
| Drawer | `src/views/views/components/DrawerDetailPage/` |
| Settings | `src/views/SettingsPopup/modules/` |
| Constructor | `src/views/Constructor/Tables/` |
| Auth | `src/views/Auth/components/LoginFormDesign/` |
| Sidebar | `src/components/LayoutSidebar/`, `src/hooks/useSidebarElements` |

Не референс — предыдущие поколения: `views/Objects/`, `views/table-redesign/`,
`components/DataTable/`, `components/FormElements*`, всё с суффиксами
`Old`, `New`, `V2`, `-backup`.

## Бэкенд — только чтение

`/Users/nurmuhammad/Documents/workspace/IT/udevs/ucode/ucode_backend/` —
`ucode_go_admin_api_gateway`, `ucode_go_object_builder_service`,
`ucode_go_auth_service`, `ucode_go_company_service`.

Читать — нужно и полезно: там истина о том, что делает ручка, что она
пишет в базу, какие поля обязательны и почему ответ выглядит именно так.
Это первый источник при вопросе «а как оно на самом деле».

**Писать — нельзя.** Ни правок, ни новых файлов, ни `sed -i`, ни `git`
с изменением состояния (`commit`, `checkout`, `stash`, `restore`).
Правка бэкенда — отдельная задача в отдельном репозитории с отдельным
ревью; из фронтового репозитория она уезжает незамеченной.

То же самое про старую админку
(`/Users/nurmuhammad/Documents/workspace/IT/udevs/ucode-frontend/`): это
спецификация поведения, а не рабочий код.

Нашлась ошибка в бэкенде — не чинить, а сказать словами: файл, строка,
что не так. Если фронт вынужден её обходить, обход помечается
комментарием со ссылкой на место в Go-коде.

`Edit` и `Write` по этим путям запрещены в `.claude/settings.json`.
Настройка ловит инструменты правки, но не любую команду в Bash —
поэтому правило записано и здесь.

## Правила

- **Границы.** `shared/` не зависит ни от чего. `features/*` импортирует только `shared/` и `index.ts` соседней фичи. `routes/` импортирует фичи, обратно — нет. Проверяется `npm run lint`.
- **Запросы** объявляются только в `features/<имя>/api/`. Компонент вызывает готовый хук.
- **Ключи кэша** — только из `shared/lib/query-keys.ts`. Строковых литералов нет.
- **Цвета** — только токены из `app/styles.css`. Захардкоженный цвет ломает тёмную тему.
- **Одно имя — один источник.** Бэкенд отдаёт одно и то же под разными именами. Нормализация — один раз, в `features/*/api/`. `a || b || c.d` в компоненте запрещён; истина ищется в схеме БД и proto.
- **Никаких legacy-веток.** `new_design`, `new_router`, `new_layout` не читаются: всегда новое поведение.

## Зависимости

**Перед установкой или добавлением любой зависимости — проверить актуальную
версию в реестре, а не писать по памяти:**

```
npm view <пакет> version          последняя стабильная
npm view <пакет> dist-tags        есть ли rc/beta/next
```

(`npm view` — это запрос к реестру, он работает и в проекте на pnpm.)

Модель помнит версии на момент обучения, и они устаревают. При первой
сборке этого проекта разрыв был на мажор почти везде: vite 6→8,
typescript 5→7, eslint 9→10, vitest 2→4, zod 3→4, react-table 8→9.

**Исключение — совместимость.** Берётся последняя стабильная, **которую
поддерживает остальной инструментарий**. Если самая свежая версия ломает
соседа, ставится последняя работающая, а причина пишется рядом.

Живой пример, который сейчас в `package.json`:

```
typescript        5.9.3   не 7.0.2
```

typescript-eslint 8.67.0 (последний стабильный) явно отказывается работать
с TS 7, а без него ESLint не парсит `.ts` вообще. Версию можно поднять,
когда выйдет стабильный typescript-eslint с поддержкой TS 7.

Бета, rc, alpha, canary — не ставим.


## Команды

Пакетный менеджер — **pnpm**, версия закреплена в `package.json`
(`packageManager`). npm и yarn не использовать: их лок-файлы разойдутся
с pnpm-lock.yaml, а плоский node_modules скроет незадекларированные
зависимости.

```
pnpm install
pnpm dev            сервер на 7777
pnpm build          tsc + vite build
pnpm lint           границы и типы
pnpm test           vitest
pnpm gen:api        типы из swagger бэкенда
pnpm gen:routes     routeTree.gen.ts
```

**pnpm не поднимает транзитивные зависимости.** Если пакет импортируется
из кода — он объявляется в `package.json` явно, а не берётся из-под
чужой зависимости. Ошибка `ERR_MODULE_NOT_FOUND` на пакете, который
«вроде установлен», означает именно это: `pnpm add -D <пакет>`.
