/**
 * Самоописание ремоута. Хост читает его ДО того, как взять страницу,
 * и по нему решает, какой React положить в общую область и куда
 * монтировать — в наше дерево или в отдельный корень.
 *
 * Модуль обязан быть без импортов: он загружается раньше, чем хост
 * заполнил общую область, и любой `import` разделяемого модуля
 * застолбил бы в `moduleCache` ремоута не ту копию React.
 *
 * Номер договора равен мажору этого пакета. Руками не пишется —
 * расходиться нечему.
 */
export const meta = { contract: 1, react: "19" };
