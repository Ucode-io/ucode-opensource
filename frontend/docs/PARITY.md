# Чек паритета со старой админкой

Как заполняется — [PLAN-v2.md](PLAN-v2.md), §5. Коротко: одна фича
за проход, поведение выписывается ИЗ КОДА старого экрана, каждая строка
получает статус. «Выброшено» без причины — это «пропущено».

Ссылки на старое ведут в
`/Users/nurmuhammad/Documents/workspace/IT/udevs/ucode-frontend/ucode_admin_frontend`,
референс — `src/views/views/` (предыдущие поколения не сравниваем).

## Auth

Референс: `src/views/Auth/` — `components/LoginFormDesign/`,
`components/RegisterFormPageDesign.jsx`, `components/RecoverPassword.jsx`,
`components/InviteForm.jsx`.

| Поведение | Где в старом | Статус | Чем закрыто |
|---|---|---|---|
| Вход логином и паролем | LoginFormDesign/LoginTab.jsx | есть | `LoginForm`, способ «Пароль» |
| Вход по коду в SMS | LoginFormDesign/PhoneLogin | есть | способ «Телефон»: `send-code-app` с `PHONE`, затем `type: "phone"` |
| Вход по коду на почту | LoginFormDesign/EmailAuth | есть | способ «Почта» — добавлен этим проходом; ручка его умеет (`session_v2.go`, случай `WithEmail`) |
| Вход через Google | ExternalAuth/GoogleAuthLogin.jsx | есть | `GoogleButton` + `type: "google"` |
| Выбор записи в connection'ах после входа | components/DynamicFields.jsx | есть | `ConnectionPicker`; варианты приходят прямо в ответе входа, отдельный `get-connection-options` не нужен |
| Выбор компании, проекта, окружения и client type диалогом | LoginFormDesign/LoginCompaniesList.jsx | выброшено | `/v3/multicompany/default-login` сам находит проект пользователя (`UserDefaultProject`), и спрашивать нечего |
| Восстановление пароля | components/RecoverPassword.jsx | есть | `RecoverForm`, все четыре шага ручек |
| Регистрация компании | components/RegisterFormPageDesign.jsx | есть | `RegisterForm` |
| Регистрация по приглашению | components/InviteForm.jsx | есть | `InviteForm` |
| Регистрация через Google | LoginFormDesign/index.jsx:158 (`register`) | выброшено | `company_service.go:78` декодирует токен без проверки подписи: чужой email забирается подделанным токеном. Вернём, когда появится проверка |
| Вход по ЭЦП (ESP) | LoginFormDesign/EspLogin/index.jsx | выброшено | это заглушка: `<Box>EspLogin</Box>` и ничего больше |
| Кнопка чата поддержки на экране входа | ExternalAuth/ChatwootLogin | выброшено | это виджет поддержки, а не способ входа; чужой скрипт на странице логина |
| OTP через Firebase | PhoneLogin/FireBaseOtp.jsx | выброшено | второй канал того же кода, со своим SDK и ключами в исходнике; ручка принимает и обычный `sms_id` |
| Флаги `new_router`, `newUi`, `newLayout`, `detailPage` в localStorage | LoginFormDesign/index.jsx:236 | выброшено | legacy-ветки не читаем вовсе — правило проекта |

## Actions (автоматизация)

Референс: `views/views/components/TableActions/TableActions.jsx`,
`views/views/components/ActionSettings/ActionSettings.jsx`,
`views/views/components/ActionsList/`.

| Поведение | Где в старом | Статус | Чем закрыто |
|---|---|---|---|
| Список действий таблицы, создание, правка, удаление | TableActions.jsx | есть | `features/table/api/actions.ts`, `TableActions` |
| Подпись по языкам данных, значок, адрес после успеха | ActionSettings.jsx:205 | есть | те же поля в нашей форме |
| Функция (`event_path`) | ActionSettings.jsx:220 | есть | выбор из функций проекта |
| Путь у функции-процесса (WORKFLOW) | ActionSettings.jsx:246 | есть | добавлено этим проходом: поле показывается, когда выбранная функция типа `WORKFLOW`; колонка `path` в `custom_event` была всегда, а задать её было нечем |
| Тип действия (HTTP / before / after) и метод | ActionSettings.jsx:257 | есть | те же списки значений |
| Перезапрос строк после успеха (`use_refresh`) | ActionSettings.jsx:276 | есть | список перезапрашивается сам |
| Выключенное действие | ActionSettings.jsx:285 | есть | пишем в колонку `disable`; старая форма писала `disabled`, и выключить действие через неё было нельзя |
| Запуск над отмеченными строками | — | есть | `useRunAction` → `/v1/invoke_function`; у старой админки в `views/` запуска не было вовсе, он остался в предыдущем поколении |
| Право роли на конкретное действие | ActionPermissionModal.jsx | есть | `isAllowed`: пустой `id` записи прав — это «никто не запрещал», а не запрет |
| Разбор ответа функции (`link`, `status: error`) | ActionButton.jsx:59 | выброшено | шлюз отвечает пустым телом (`function.go:557`) — ни ссылки, ни статуса; сообщение теперь «действие запущено», а не «выполнено». См. [backend-notes](backend-notes.md), «Функции» |
| `attributes.use_no_limit` | ActionSettings.jsx:281 | выброшено | в бэкенде такого параметра нет вовсе (`no_limit` не встречается ни в шлюзе, ни в сервисах): переключатель ничего не менял |
| Дополнительные параметры функции (`additional_parameters`) | ActionsList/TableRow.jsx | пропущено | значения сохраняем целыми при правке, но не показываем: у параметров три типа (TABLE, OBJECTID, HARDCODE) со связанными списками, и редактор без разбора их семантики запишет мусор в поле, которое сейчас хотя бы не теряется |
| Вызов микрофронтенда вместо функции | ActionButton.jsx:47 | сделано | действие открывает микрофронтенд окном поверх таблицы. Ветка — по ТИПУ функции из подзапроса списка действий (`custom_event.go:241`), а не по `attributes`. Микрофронтенды подмешаны в выбор функции вторым списком: `GET /v2/function` их не отдаёт никогда |

## Раскладка карточки

Референс: `views/views/components/HeaderFilter/components/LayoutComponent/`
и `views/Constructor/Tables/Form/Layout/` (`NewSection`, `NewSectionsBlock`,
`SummarySection`, `NewlayoutList`). Не референс — `views/Objects/LayoutSettings`.

| Поведение | Где в старом | Статус | Чем закрыто |
|---|---|---|---|
| Порядок полей в карточке перетаскиванием | NewSection.jsx (`applyDrag`) | есть | `model/layout`, `moveField` — с переносом В СЕКЦИЮ цели |
| Поле-заголовок карточки (`layout_heading`) | SettingsBlock.jsx | есть | `setHeading`, у мультиязычной таблицы — карта «язык → слаг» |
| Секции: завести, переименовать, удалить | NewSectionsBlock.jsx | есть | прямо в карточке; поля удалённой секции уходят в соседнюю, последнюю удалить нельзя |
| Имя секции переживает перезапрос | — | есть | наш обход: имя дублируется в `attributes.label`, потому что колонку `label` не возвращает ни один GET ([backend-notes](backend-notes.md)) |
| Поле спрятано из карточки (`field_hide_layout`) | PageSettings/index.jsx:139 | есть | `toggleHidden` — «глаз» у поля в режиме правки карточки; колонкой таблицы то же поле остаётся. Поля нет в раскладке — оно дописывается в последнюю секцию спрятанным, иначе «глаз» у только что заведённого поля молчал бы |
| Правка раскладки отдельным режимом | views/Objects/LayoutSettings (целый экран) | есть | кнопка с карандашом справа от вкладок карточки: включает имена секций, «глаз» у полей и постоянные ручки перетаскивания. Отдельного экрана нет — правится там же, где видно результат |
| Права роли на поля (`field_permission`) | — | есть | `fieldRights` + `applyRights`: запрет строгий, разрешение по умолчанию |
| Вкладки связей: показать и убрать | RelationsBlock.jsx | есть | вкладка — это view пункта меню (`features/view/model/relation-tabs`), а не запись в раскладке: «+» в полосе вкладок и «⋯» у вкладки |
| Сводная секция (`summary_section`) | SummarySection.jsx | выброшено | сервер не отдаёт её ни под одним именем: `is_summary_section` нет в запросе секций, а `summary_fields` не собирают ни `GetAllV2`, ни `GetSingleLayout` — заполняет их одна мёртвая `GetAll` (`layout.go:763`), в которую grpc не заходит ([backend-notes](backend-notes.md)). В КАРТОЧКЕ старой админки её тоже нет — только константа в `defaultValues`, а в drawer'е строка закомментирована (`DrawerDetailPage/index.jsx:156`); живой редактор есть в конструкторе (`Constructor/Tables/Form/Layout/SummarySection.jsx`) — там же и вернётся к вопросу |
| Список раскладок таблицы, назначение раскладки пункту | NewlayoutList.jsx, LayoutsItem.jsx | пропущено | это экран конструктора таблицы, а не настройка карточки; вдобавок список не отдаёт `menu_id`, а `Layout().GetByID` шлюз наружу не выводит (зовёт только внутри `UpdateLayout`). В старой админке этот экран и сам сломан: `NewlayoutList.jsx:128` группирует по `layout?.menu_id`, которого в ответе нет. В проходе Constructor |
| Флаги раскладки: `is_default`, `is_modal`, `is_visible_section` | NewlayoutList.jsx:43–89 | пропущено | там же: они правятся из списка раскладок |
| Значок вкладки | LayoutTabs.jsx | выброшено | GET его не отдаёт, а PUT пишет колонку безусловно — значок стирался при любой правке раскладки |

## Шаблоны

Референс: `layouts/MainLayout/TemplateMenu/` (`index.jsx`, `TemplateTables.jsx`,
`TemplateSelection.jsx`), `services/templateService.js`,
`services/menuTemplateService.js`.

| Поведение | Где в старом | Статус | Чем закрыто |
|---|---|---|---|
| Список шаблонов проекта | TemplateSelection.jsx | есть | `TemplateDialog`, `GET /v1/template` |
| Развернуть шаблон в проект | TemplateSelection.jsx (`execute`) | есть | `useApplyTemplate`; после успеха меню перезапрашивается целиком |
| Сохранить папку шаблоном: имя, описание, таблицы | TemplateMenu/index.jsx (`onSubmit`) | есть | `TemplateCreateDialog`, `POST /v1/template` с `menu_id` папки |
| «Со строками» у таблицы | TemplateTables.jsx:56 | частично | флажок один на шаблон: при смешанном выборе шлюз отдаёт таблице без флага чужие строки (`template.go:106`, `345`) |
| Выбор отдельных таблиц ВНУТРИ шаблона при развёртывании | TemplateSelection.jsx | выброшено | `execute` читает из тела только `id` и `tables` целиком (`template.go:29`): выбор ничего не менял |
| Картинка шаблона | PhotoUpload.jsx, TemplateImage.jsx | выброшено | шаблон выбирают по имени в списке из нескольких строк; 400 строк редактора картинки на это не нужны. Поле `photo` в ответе читаем и показываем, если оно заполнено |
| Функции и микрофронтенды в шаблоне | FunctionsTable.jsx, MicroFunctions.jsx | выброшено | ключи шлюз кладёт в шаблон как есть, но собрать их было неоткуда: в старой форме оба списка всегда уходили пустыми |
| Вторая ручка `/v1/menu-template` | menuTemplateService.js | выброшено | параллельная ручка того же самого; старая админка выбирала между ними по состоянию всплывающего меню. Дерево пункта и так уезжает по `menu_id` |

## Микрофронтенды

Референс: `components/MicrofrontendComponent/index.jsx`,
`views/Microfrontend/index.jsx`, `layouts/MainLayout/MicrofrontendLinkModal.jsx`.
Решение о способе встраивания — [ADR-0005](adr/0005-microfrontend-embedding.md).

| Поведение | Где в старом | Статус | Чем закрыто |
|---|---|---|---|
| Пункт меню открывает чужое приложение | MenuSwitchCase.jsx:20 | есть | `features/microfrontend`, ветка в маршруте пункта |
| Адрес сборки: `https://<url>/assets/remoteEntry.js` | Microfrontend/index.jsx:30 | есть | `entryUrl`; схему не удваиваем, хвостовую косую снимаем |
| Загрузка модуля `./Page` федерацией | MicrofrontendComponent/index.jsx:23 | есть | `@module-federation/runtime`, `type: "module"`; рантайм грузится по требованию |
| Перемонтирование при каждом заходе (`activationKey`) | MicrofrontendComponent/index.jsx:31 | есть | ключ из id и адреса |
| Пропсы: окружение, `i18n`, оси http | MicrofrontendComponent/index.jsx:59 | есть | те же имена; вместо axios 0.26 — наши `http` и `httpAuth`. Форма ответа отличается: наш перехватчик снимает конверт |
| Заведение пункта: выбор приложения и параметры | MicrofrontendLinkModal.jsx | есть | «+» → «Создать микрофронтенд»: список приложений проекта и редактор пар «ключ — значение» |
| Смена приложения у готового пункта | MicrofrontendLinkModal.jsx (правка) | выброшено | `PUT /v3/menus` колонку `microfrontend_id` не пишет вовсе (`menu.go:944`) — список в форме правки был бы переключателем, который ничего не переключает |
| `attributes.params` в query-строке адреса | MenuSwitchCase.jsx:25 | частично | передаём их пропсом `params`, а не через адрес: у нас пункт живёт по `/m/$menuId`, и своей строки запроса у ремоута нет. Ремоут, читающий `location.search`, их не увидит |
| Общие `react`, `react-dom`, `react-router-dom` | vite.config.js:25 | выброшено | ремоуты собраны против React 18, у нас 19; общий синглтон отдал бы им нашу версию. У каждого свой React — штатный запасной путь федерации |
| Отдельная страница-песочница для ремоута | views/MicrofrontendPlayground | выброшено | поле ввода для произвольного `remoteEntry.js` — инструмент отладки, а не экран админки |
| Микрофронтенд на экране входа | layouts/AuthLayout/LoginMicrofrontend.jsx | сделано | другое место монтирования и другой договор пропсов — заведён отдельным типом `LoginRemoteProps` (ADR-0005). Привязка к поддомену настраивается внизу раздела «Микрофронтенды». Сверх старой админки: наша форма осталась запасным путём — упавший ремоут больше не отнимает вход. На localhost запрос не уходит (там подставлялся зашитый чужой поддомен) |
| Экран конструктора: список, версии, промоут, откат | SettingsPopup/modules/MicroFrontend | частично | раздел «Микрофронтенды» в настройках: список, заведение, удаление, версии со снимком файлов и откатом, публикация с ожиданием сборки. Правки записи нет — `PUT` сломан в обе стороны; редактора файлов нет — шлюз не проксирует `files` и `push-changes`. Обе причины в [backend-notes](backend-notes.md) |

## Настройки меню

Референс: `layouts/MainLayout/` (`TableCreateModal`, `FolderCreateModal`,
`LinkTableModal`, `MenuSettingModal`), `services/menuSettingService.js`.

| Поведение | Где в старом | Статус | Чем закрыто |
|---|---|---|---|
| Имя пункта по языкам, значок | FolderCreateModal.jsx:141 | есть | `MenuFormDialog` |
| Слаг таблицы при создании | TableCreateModal.jsx:304 | есть | шаг с ключом; таблица заводится ручкой `/v1/table` |
| Адрес у пункта-ссылки | LinkTableModal.jsx:177 | есть | поле адреса, проверка протокола (`safeHref`) |
| Пункт-ссылка на СУЩЕСТВУЮЩУЮ таблицу (`type: LINK` + `table_id`) | LinkTableModal.jsx:53 | есть | добавлено этим проходом: такой пункт открывается таблицей, а не объяснением «экрана нет» (`showsTable`) |
| Настройки самой таблицы: кэш, мягкое удаление, вход, сортировка | TableCreateModal.jsx:326 | есть | страница «Настройки таблицы» в панели view |
| Вид сайдбара: тип меню, размер пунктов, шаблон | MenuSettingModal.jsx, `/menu-settings` | выброшено | у сайдбара один вид, заданный дизайн-системой |
| Права роли на пункт (`menu_permission`) | SettingsPopup/PermissionsRoleDetail | есть | вкладка «Меню» в правах роли: дерево грузится по уровню, уезжают только изменённые пункты (см. раздел «Настройки (Settings)») |
| Системные пункты: избранное, аналитика, сводная, отчёты | STATIC_MENU_IDS | частично | прячем только настройки, файлы и пользователей — у них свои экраны; остальные видны и открываются объяснением, пока экранов нет ([PLAN-v3](PLAN-v3.md), §1) |

## Sidebar

Референс: `src/components/LayoutSidebar/` (`index.jsx`,
`SidebarRecursiveBlock/RecursiveBlockComponent.jsx`, `MenuButtons.jsx`),
`src/hooks/useSidebarElements/`.

| Поведение | Где в старом | Статус | Чем закрыто |
|---|---|---|---|
| Дерево пунктов меню с вложенными папками | SidebarRecursiveBlock/RecursiveBlockComponent.jsx | есть | `MenuLevel`, рекурсивно |
| Пункт скрыт без права чтения | hooks/useSidebarElements/index.jsx:19 | есть | `toNodes` отбрасывает `can.read === false` |
| Перетаскивание пунктов и папок | index.jsx (`react-smooth-dnd`, `applyDrag`) | есть | `model/reorder.ts` — чистая функция с тестами, порядок пишется `PUT /v3/menus` |
| Раскрытые папки помнятся между заходами | store/menus (menuAccordionActions) | есть | `expandedMenus` в `ui-store`, забывается вместе с удалённым пунктом |
| Свой значок пункта (SVG по ссылке) | LayoutSidebar/MenuIcon.jsx | есть | `shared/ui/dynamic-icon`, запасной значок по типу |
| Создание таблицы, папки и ссылки | index.jsx (TableCreateModal, FolderCreateModal, LinkTableModal) | есть | `AddMenuButton` и меню строки |
| Пункт на УЖЕ СУЩЕСТВУЮЩУЮ таблицу («Add table») | layouts/MainLayout/TableLinkModal.jsx | есть | «Существующая таблица» в «+» и в «⋯» папки: выбор из `useTables` с поиском, `POST /v3/menus` с `table_id`. Свои view (TABLE и SECTION) пункту заводит бэкенд (menu.go:113) |
| Кнопка создания и перетаскивание — по глобальному праву роли | index.jsx:163 (`menu_drag`), :612 (`menu_button`) | есть | `useGlobalRight`; права приезжают ответом логина (`global_permission`) и лежат в сессии. Записи нет вовсе — разрешаем: это «не приехало», а не «нельзя» |
| Поиск по пунктам | AppSidebarComponentV2.jsx:92 (`search` у `/v3/menus`) | есть, шире | у старого — только внутри одной раскрытой папки, и вхолостую: `search` бэкенд принимает, но в SQL не доносит (см. backend-notes). У нас поиск по ВСЕМУ дереву: оно дочитывается уровнями при первом наборе, отбор идёт в памяти, по подписям на всех языках проекта. Щелчок раскрывает дорогу до найденного |
| Переименование и удаление пункта | RecursiveBlockComponent.jsx:589 | есть | `MenuRowActions` |
| Сворачивание сайдбара | index.jsx (KeyboardDoubleArrow…) | есть | `CollapseButton` плюс подглядывание по наведению |
| Переключение проекта и окружения | index.jsx (useCompanyListQuery, useEnvironmentListQuery) | есть | `WorkspaceSwitcher` в шапке |
| Выход, профиль, язык интерфейса | index.jsx (Logout, TranslateIcon) | есть | меню шапки: профиль, язык, тема, выход |
| Ширина сайдбара тянется мышью | — | есть | наше добавление, у старого ширина постоянная |
| Создание wiki, wiki-папки, микрофронтенда, minio-папки, сайта | RecursiveBlockComponent.jsx:183–218 | частично | заводим то, у чего есть экран: папка файлов, встроенная страница (флажок у ссылки) и микрофронтенд. Wiki не заводим — экрана за ней нет и не будет, пока нет ручек. Пункт, ведущий на заглушку, заводить незачем |
| «Настройки пункта» (вид сайдбара: размер, стиль) | layouts/MainLayout/MenuSettingModal.jsx | выброшено | у сайдбара один вид, заданный дизайн-системой; настройка меняла размеры и стиль пунктов |
| «Сделать шаблоном» из папки | layouts/MainLayout/TemplateMenu | есть | `TemplateCreateDialog` в «⋯» папки — см. раздел «Шаблоны». Роль спрашивается в самом меню строки (`useIsSuperRole`): пока это был проброшенный флаг, строка передавала голое `false`, и действия не видел никто |
| «Переместить в папку» отдельным окном | MenuButtons.jsx:270 (`Move table`) | выброшено | перетаскивание делает то же самое и показывает результат сразу; в старой админке кнопка к тому же была привязана к одному захардкоженному родителю |
| Кнопки разделов: API, функции, запросы, сценарии, документы, Minio, права | LayoutSidebar/MenuButtons.jsx | пропущено | это отдельные экраны конструктора, не сайдбар; их черёд в обходе — Constructor и Settings |
| Приглашение пользователя из сайдбара | components/InviteModal | есть | но не из сайдбара, а в настройках, разделом «Пользователи»: приглашение — это ссылка, и рядом с ней должен быть список тех, кто по ней уже пришёл |
| Создание организации | LayoutSidebar/AddOrganization.jsx | пропущено | вместе с экраном компаний |
| Баннер подписки | utils/subscriptionWarning | выброшено | биллинга в проекте нет |

## TIMELINE

Референс: `src/views/views/modules/Timeline/`.

| Поведение | Где в старом | Статус | Чем закрыто |
|---|---|---|---|
| Полоса от `calendar_from_slug` до `calendar_to_slug` | TimeLineDayDataBlockItem.jsx:176 | есть | `model/timeline.ts`, `timelineBar` |
| Лента месяцев без конца, подгрузка у краёв | hooks/useDateLineProps.jsx:66 | есть | `onLoadPast`/`onLoadFuture`, ±1 месяц за шаг вместо ±3 |
| Поправка прокрутки при росте ленты влево | useDateLineProps.jsx:101 | есть | `anchored` в `Timeline.tsx` |
| Масштаб оси: дни, недели, месяцы | TimeLineBlock.jsx:217 | есть | Те же три. «Недели» у старого отличались от «дней» только подписью в шапке (ширина колонки та же, 60px) — у нас колонка вдобавок уже, иначе средний масштаб ничего не даёт |
| Прокрутка к сегодня на открытии и смене масштаба | TimeLineDayBlock.jsx:93 | есть | `focus` + кнопка «Сегодня»; при смене масштаба возвращаемся не на сегодня, а на день у левого края |
| Подпись месяца едет с прокруткой | TimeLineDatesRow.jsx:159 | есть | `sticky` подпись в шапке |
| Выходные другим фоном (кроме масштаба месяца) | TimeLineDayBlock.jsx:89 | есть | слой колонок в `Timeline.tsx` |
| Отметка «сегодня» в шапке | TimeLineDayBlock.jsx:86 | есть | кружок на числе + подложка колонки |
| Подсветка дней полосы в шапке на наведении и переносе | TimeLineDayDataBlockItem.jsx:312 (`setFocusedDays`) | есть | `focused` в `Timeline.tsx` |
| Подсветка строки на наведении | TimelineBlockProvider (`hoveredRowId`) | есть | `group/row` в разметке |
| Перенос полосы по дням, PUT обеих дат | TimeLineDayDataBlockItem.jsx:230 | есть | `drag.kind === "move"` |
| Растягивание за левый и правый край | Moveable `renderDirections: ["w","e"]` | есть | ручки `EventChip`, `drag.kind === "resize"` |
| Прилипание к суткам при переносе | `throttleDrag` | есть | день считается из `dayAt`, дробных дней нет |
| Щелчок по полосе открывает запись | TimeLineDayDataBlockItem.jsx:70 | есть | `onOpenRow` |
| Свой адрес перехода (`attributes.navigate`) | Timeline.jsx:110 | есть | `openRowUrl` в маршруте |
| Список записей слева сворачивается | components/Sidebar + SidebarButton | есть | кнопка сворачивания; у нас список есть всегда, у старого — только при группировке |
| Записи без дат видны и ставятся на ось | TimeLineBlock.jsx:161, TimeLineDataRecursiveRow.jsx:78 | есть | раздел «Без дат» + протяжка. Старый ставил `сегодня…+5 дней` по щелчку — у нас даты берутся из протяжки |
| Поля, показанные на полосе | ColumnsVisibility/useColumnsVisibilityProps.jsx:84 (`attributes.visible_field`) | есть | пишем `columns`, а `visible_field` читаем запасным вариантом ради старых view (`resolveColumns`) |
| Подсказка с датами при протяжке | components/TimelineRowNewDateLine | есть | подпись у выделения и у призрака |
| Настройка полей дат | HeaderFilter/components/TimelineSettings | есть | тот же `CalendarFields`, что у календаря |
| Одинаковые поля начала и конца | TimeLineBlock.jsx:61 (красное окно, экран пуст) | выброшено | такой выбор просто не предлагается в списке |
| Однодневная задача | TimeLineDayDataBlockItem.jsx:387 (`differenceInDays > 0` — полоса скрыта) | выброшено | у нас минимум одна колонка: скрывать запись, у которой начало равно концу, нечестно |
| Группировка: разделы слева, свёртка, вложенность | components/TimelineRecursiveRow, Sidebar | есть | Столько уровней, сколько полей в `attributes.group_by_columns`, той же разбивкой, что у таблицы (`model/group`); отступ по уровню |
| «Expand all / Collapse all» над группами | Sidebar.jsx:47 | выброшено | Появлялась только при двух уровнях и более; на одном сворачивать нечего оптом |
| Заголовок списка слева («Columns») | Sidebar.jsx:44 | есть | «Записи» |
| «＋ New» внизу списка | Sidebar.jsx:100 | есть | пустая строка внизу: по ней протягивают даты новой записи |
| Свой запрос `gte`/`lte` в теле | Timeline.jsx:149 | выброшено | ключи не разбираются бэкендом (`build_query.go:256`, `applyFilters`): отбора по диапазону у старого экрана нет вовсе, приезжает вся таблица. У нас — `{поле: {$gte,$lte}}` |
| `view_type: "TIMELINE"` в теле | Timeline.jsx:148 | выброшено | ручка берёт из тела только ключи-слаги полей (`object_builder.go:1249`) |
| `builder_service_view_id` (серверная группировка) | Timeline.jsx:152 | выброшено | ветка игнорирует фильтры, права и страницу — [backend-notes](backend-notes.md), «Группировка» |
| Всплывающая подсказка на клетке шапки | TimeLineDayBlock.jsx:152 | есть | `title` с полной датой; день недели вдобавок написан в самой шапке |
| Число в узком масштабе — не у каждого дня | TimeLineMonth.jsx:88 | есть | понедельник, сегодня и края подсвеченного отрезка |
| Подсказки по наведению (день оси, полоса, кнопки) | TimeLineDayBlock.jsx:152 (MUI Popover) | есть | свой `shared/ui/tooltip`, а не браузерный `title` |
| Зум-множитель `zoomPosition` | TimeLineBlock.jsx:234 | выброшено | у старого он выводится из масштаба и другого значения не принимает — это не настройка, а константа |

## Constructor: таблица, поля, связи

Где смотреть: `views/Constructor/Tables/Form/NewRouterTable.jsx` — шесть
вкладок («Details», «Layouts», «Fields», «Relations», «Actions», «Custom
errors»), плюс `TablesList.jsx` и `ImportModal.jsx`. Не референс:
`OldRouterTableSettings.jsx`, `Layout-backup/`, `Actions-backup/`,
`views/Objects/`.

Отдельного экрана конструктора у нас нет намеренно: настройки таблицы
и полей открываются там, где на них смотрят, — в панели view
(`ViewOptions` → `TableSettings`) и в панели поля у заголовка колонки
(`FieldEditor`). Экран, ради которого уходят со своих данных, — это
и есть причина, по которой в старой админке настройки разошлись
с тем, что видно.

Список полей в панели view — один, а не два: строка «Свойства» держит
и показ колонки (глаз, порядок), и настройки самого поля (правка,
удаление) — как в старой админке, где у строки «Visible columns» справа
стоит меню поля (`ColumnsVisibility.jsx:172` → `FieldOptions.jsx`).
Отдельная страница «Поля таблицы» у нас была и убрана: одно и то же
поле искали в двух местах панели.

**Пройдено этим проходом:** все шесть вкладок, список таблиц и импорт.
Настройки связи сверены по колонкам таблицы `relation`
(`000001_init_tables.up.sql:104`), а не по форме старой админки: форма
показывает и то, чего ни одна ручка не пишет.

| Что | Где в старом | Статус | Как у нас |
|---|---|---|---|
| Имя таблицы по языкам данных | MainInfo.jsx:117 | есть | `LanguageInput` в `TableSettings` |
| Слаг таблицы | MainInfo.jsx:132 | выброшено | `UPDATE "table"` колонки `slug` не пишет (`table.go:887`): поле принимало новый слаг и отвечало «сохранено», не меняя ничего. Показан, но не правится |
| Кэш, мягкое удаление, ручная сортировка | MainInfo.jsx:157–172 | есть | те же три флажка |
| Таблица входа + способы входа | MainInfo.jsx:148, 336 | есть | включение ждёт первого способа: ручка отказывает пустому `login_strategy` (`table.go:970`) |
| Шесть списков `auth_info` (тип клиента, роль, логин, пароль, email, телефон) | MainInfo.jsx:197–308 | выброшено | бэкенд собирает `auth_info` сам и перезаписывает присланное (`table.go:1007`) |
| Поле: тип, слаг, подпись по языкам, обязательность, уникальность, только чтение, проверка и текст отказа, значение по умолчанию, автозаполнение, мультиязычность | FieldSettings.jsx:463–845 | есть | `FieldEditor` + `model/field-draft`; правки копятся в черновике и уезжают одним PUT |
| Атрибуты по типам: приставка и разрядность номера, точка карты, варианты списка, формула, агрегат с отбором | Fields/Attributes/* | есть | `TypeSettings`, `FormulaSettings`, `AutofillSettings`, `ButtonSettings` |
| Ключ карт у каждого поля MAP и «формат снимка» у PHOTO | Attributes/MapAttributes.jsx, ImageAttributes.jsx | выброшено | обе настройки не читает никто: ключ у нас свой и зашит в `shared/lib/yandex-maps`, `format` не разбирает даже ручка загрузки — снимок кодируется в том формате, в котором пришёл (`file.go:147`). Уже записанные значения в attributes остаются (FIELD-AUDIT, F21 и F25) |
| Тип `MONEY` | fieldTypes.js:129 | выброшено | мёртвая зона: ввести значение нечем (редактора нет ни в одном поколении), а ячейка не рисуется вовсе — `CellElementGenerator.jsx:278` возвращает разметку ТОЛЬКО когда сумма пуста. Настроек валюты нет: в базе это `TEXT[]` (`pkg/helper/convert.go:46`). Деньги сегодня — `FLOAT` рядом со списком валют |
| Тип `PERSON` | fieldTypes.js:286 | выброшено | это не тип, а ярлык: `CreateField` при `PERSON` заводит Many2One на системную таблицу `person` и выходит, не вставляя поле (`field.go:70–102`), а сама колонка получает тип `LOOKUP`. Наша форма связи делает ровно это и явно |
| Типы, закомментированные в старом списке (`PROGRAMMING_LANGUAGE`, `BARCODE`, `CODABAR`, `SCAN_BARCODE`, `DENTIST`, `AUTOFILL`) | fieldTypes.js:101–163 | выброшено | заводить их нельзя и в старой админке. Уже заведённые читаются: `model/cell-kind` знает их все, кроме `DENTIST` — тот показывается текстом |
| Скрыть поле по значению другого (`attributes.hide_path`) | FieldSettings.jsx:875–949 | сделано | настройка есть в обеих формах старой админки, а ПРИМЕНЯЕТ её только `views/Objects/NewMainInfo.jsx:45` — предыдущее поколение; в нынешней карточке её не читает никто. У нас читает: `item/model/visibility` плюс `table/ui/VisibilitySettings`. Имя обманывает — совпало условие, поле ПОКАЗЫВАЕТСЯ. Исправлено два: сравнение по строковому виду вместо `===` (иначе правило на числе не срабатывало никогда) и набор как множество вместо поэлементного |
| Вкладка «Custom errors» | CustomErrors/index.jsx | заблокировано | `/v2/collection/{slug}/error_messages` в шлюзе нет: ни маршрута в `api/api.go`, ни обработчика — только proto и сгенерированный код. Старая админка зовёт несуществующую ручку |
| Вкладка «Layouts» | Form/Layout/ | см. «Раскладка карточки» | секции и поля правятся в карточке; список раскладок и назначение пункту заблокированы ответом сервера |
| Вкладка «Actions» | Form/Actions/ | есть | см. раздел «Actions» |
| Связь: целевая таблица и поля показа | Relations/RelationCreateForm.jsx | есть | панель поля, тип «Связь»; направление одно — Many2One (`model/relation-draft`) |
| Автофильтр связи (`auto_filters`) | Relations/AutoFiltersBlock.jsx | есть | пары «поле этой записи → поле чужой таблицы» задаются в форме связи и применяются при выборе строки (`model/relation-draft` + `autoFilterValues`). Раньше настройка не читалась вовсе и приезжала вся чужая таблица |
| Правится ли связь из таблицы (`editable`) | RelationSettings.jsx | выброшено | колонку `relation.editable` в нынешнем поколении не читает НИКТО: живёт только `view.attributes.table_editable` (`TableDataForm.jsx:76`), а это настройка view, и она у нас есть |
| Подставлять «своего» в связь (`is_user_id_default`, `object_id_from_jwt`) | Relations/DefaultValueBlock.jsx | есть | в форме связи — три состояния списком вместо двух флажков: значение уезжает одно, а поднять в старой форме можно оба. Подставляется при заведении записи И карточкой, и строкой в подвале (`model/relation`, `selfDefaults`) |
| «Свой ID» и «ID из токена» — это разные идентификаторы | FormElementGenerator.jsx:126 | есть | первый — вошедший (`user_id`), второй — ЕГО строка в целевой таблице, выданная входом (claim `tables`, см. [[App Table]]). У курьера есть и то и другое, и связь на «Курьера» должна получить второе |
| Подмешивать свою строку в список выбора связи | RelationField.jsx:128 (`additional_request`) | выброшено | это приём против пагинации: старый список приходит по 10 строк, и выбранной в нём может не оказаться. У нас подпись связанной строки читается из `<поле>_data`, приехавшего вместе со строкой, — искать её в списке не нужно |
| Каскадные списки (`cascadings`) | CascadingRelationSettings.jsx | сделано | цепочка собирается в правке связи, выбор идёт по шагам с крошками. Формат прочитан: звенья лежат ближним концом вперёд, а пара внутри звена разъезжается на шаг. Дерево кандидатов собираем из схемы — ручка `/v2/relations/{slug}/cascading` на postgres отвечает пустотой (`relation.go:79`). Подписи промежуточных шагов берём у связи уровнем ниже, а не из `item.name`, как старая админка. Цепочка не от этой связи не применяется |
| Дерево каскада (`cascading_tree_table_slug`, `cascading_tree_field_slug`) | CascadingTreeBlock.jsx | пропущено | вторая, отдельная настройка: не цепочка таблиц, а рекурсивное дерево В ОДНОЙ таблице. Отдельная фича, и у каскада с ней общего только имя колонки |
| Many2Dynamic (`dynamic_tables`) | DynamicRelationsBlock.jsx | заблокировано | связь на таблицу, выбираемую строкой. На postgres не работает у самого бэкенда: создание не заводит ни колонки, ни поля (`relation.go:704`), раскладка отдаёт `dynamic_tables` Go-строками вместо объектов (`layout.go:660`), а ветка в `GetSingleViewForRelation` собрана с лишней запятой в SQL (`relation.go:2776`). Обойти на фронте нечем — отсутствующей колонки не обойдёшь |
| Список таблиц проекта, создание, удаление | TablesList.jsx | есть | таблицы заводятся из сайдбара («+» → «Таблица»), удаляются в `TableSettings`; отдельного списка нет — им служит само дерево пунктов |
| Импорт таблиц из другого приложения | ImportModal.jsx | заблокировано | всё это про сущность «приложение»: `applicationService` зовёт `/v1/app`, а такого маршрута в шлюзе нет вовсе. Вместе с ним мертвы `is_own_table` и `is_visible`, по которым `TablesList.jsx:88` выбирает, удалять таблицу или всего лишь отвязать её от приложения |

## TABLE view

Где смотреть: `views/views/modules/Table/` (`useTableProps.jsx`,
`DynamicTable.jsx`, `components/Th/`), `components/Header/`,
`components/HeaderFilter/`, `components/ScreenOptions/`. Не референс —
`components/DataTable/`, `modules/Grid/` (ag-grid) и всё в `views/Objects/`.

**Проход не закончен.** Сверено: шапка над таблицей, сама таблица,
её подвал, панель настроек view, панель колонок, строка таблицы
и инлайн-заведение записи. Не смотрели: всплывашки сортировки и поиска
(`SortPopover`, `SearchPopover`), меню «Ещё» у переполненных вкладок
(`MoreViews`) и редакторы ячеек по типам (`ElementGenerators`).
Отсутствие строки здесь означает «ещё не смотрели», а не «нет
расхождений».

| Поведение | Где в старом | Статус | Чем закрыто |
|---|---|---|---|
| Вкладки view, «+» для нового, переполнение | HeaderFilter.jsx:123, MoreViews.jsx | есть | `ViewTabs` + `ViewCreateButton`; при переполнении полоса прокручивается, а не прячет хвост в меню «Ещё» — искать вкладку в двух местах хуже, чем крутить одно |
| Поиск, сортировка, отбор, настройки view | HeaderFilter.jsx:220–250 | есть | `TableToolbar` + `ViewOptions`; поиск разворачивается на месте, а не занимает ширину постоянно |
| Кнопка «Обновить» | HeaderFilter.jsx:227 | есть | добавлена этим проходом. Строки меняет не только тот, кто на них смотрит: приложение заказчика, функция, импорт — а кэш держит их минуту |
| Быстрые фильтры вида (`quick_filters`) | HeaderFilter.jsx:329 (`FiltersList`) | есть | подсказка админа разворачивается пустыми чипами в подшапке (`seedFilters`) |
| «Создать запись» кнопкой | HeaderFilter.jsx:308 | есть | карточкой, а строкой — в подвале таблицы; адрес из настроек view (`url_object`) важнее карточки |
| Правка ячейки на месте | useTableProps.jsx:156 | есть | у нас уезжает ОДНА колонка, а не вся строка: старый собирает объект из всех ячеек (`rowToObject`) и шлёт его целиком — то есть переписывает и то, чего не трогали, значениями с экрана |
| Отметить строки, удалить отмеченные | DynamicTable.jsx:373 | есть | `GridFooter`, та же кнопка у правого края |
| Своё число строк на странице | DynamicTable.jsx:323 (`CreatableSelect`) | есть | ввод своего значения в подвале, с границами 1…1000: «99999» в старом поле роняет вкладку |
| Номера страниц и счётчик «из N» | DynamicTable.jsx:350 | есть | плюс режим бесконечной прокрутки, если так настроен view |
| «+» колонка для нового поля | DynamicTable.jsx:180 (`FieldButton`) | есть | тот же «+» у правого края шапки, `FieldEditor` |
| Итог по колонке (`attributes.summaries`) | Th.jsx:99, DataTable/SummaryRow.jsx | выброшено | мёртвая зона: меню итога в старой админке не открывается ВООБЩЕ — `setSummaryOpen` не зовёт никто, чеврон открывает настройки поля. А сам итог всегда СУММА, какую бы формулу ни выбрали: `SummaryRow` не читает `formula_name`. Колонка в базе остаётся, уже записанные значения не трогаем |
| Как открывается карточка: сбоку, по центру, во весь экран | ScreenOptions/useScreenOptionsProps.jsx | выброшено | выбор нигде не хранится (обычный `useState` в контексте view) и виден только внутри связи; после перезагрузки страницы всё равно «сбоку». Карточка у нас открывается сбоку всегда |
| Панель настроек view: имя, тип, колонки, отбор, группировка, вкладки, закрепление, адреса, удаление | ViewOptions.jsx | есть | `ViewOptions` — те же страницы с возвратом внутри одной панели |
| Панель колонок: поиск, порядок перетаскиванием, глаз, «скрыть все», меню поля | ColumnsVisibility.jsx, FieldsOptions/FieldOptions.jsx | есть | одна страница «Колонки»; меню поля открывает ту же панель, что и заголовок колонки |
| Дорожки доски (`attributes.sub_group_by_id`) | SubGroup/BoardSubGroup.jsx, modules/Board/BoardColumn.jsx:79 | есть | добавлено этим проходом: колонки повторяются для каждого значения второго поля, перенос карточки пишет оба поля сразу. Пункт показывается только у доски — как и в старом (ViewOptions.jsx:296) |
| Отдельный экран настроек таймлайна | TimelineSettings/TimelineSettings.jsx:35 | есть | это те же два поля дат, что у календаря; строка настроек одна на оба типа, а не два экрана с одинаковой парой полей |
| Окно «General»: имя по языкам, навигация, удаление view | ViewSettingsModal/components/ViewForm/ViewForm.jsx:296 | есть | разложено строками той же панели; отдельного окна поверх панели нет |
| Номер строки, флажок, удаление у правого края | TableRow.jsx:105 | есть | номер сквозной по страницам — `(страница − 1) × размер + место`, как и в старом |
| Заведение записи строкой: Enter сохраняет | AddNewData.jsx:79 | есть | черновик в подвале таблицы; обязательные поля проверяются до отправки |
| Отбор по типу поля: связь, варианты, дата, флаг, текст | FilterGenerator/index.jsx:74 | есть | плюс числовой: в старой админке ветка NUMBER закомментирована целиком (`:127`) |
| Хлебные крошки при переходе по связям | Header.jsx:64 | есть | крошки в карточке записи (см. раздел «Раскладка карточки»), а не в шапке таблицы |
| Кнопка помощника (AI) в шапке таблицы | Header.jsx:113 | есть | `CopilotButton`, но в сайдбаре: помощник про проект, а не про одну таблицу |

## Drawer (карточка записи)

Где смотреть: `views/views/components/DrawerDetailPage/` — `index.jsx`
(окно), `DrawerFormDetailPage.jsx` (форма), `HeadingOptions.jsx`
(заголовок). Не референс — `views/Objects/ModalDetailPage/`.

Редакторы значений по типам и поле связи — соседним разделом
(«Редакторы значений и поле связи»): они же закрывают и обход
Relation-полей.

| Поведение | Где в старом | Статус | Чем закрыто |
|---|---|---|---|
| Карточка справа, тянется за край, ширина запоминается | index.jsx:91 | есть | `ResizeHandle`; ширина в общем состоянии интерфейса, а не своим ключом localStorage |
| Три способа открытия: сбоку, по центру, во весь экран | ScreenOptions/, DRAWER_VIEW_TYPES | есть | `drawerMode`, и он переживает перезагрузку — в старой админке это обычный `useState` в контексте view, то есть выбор терялся при первом же обновлении страницы |
| Заголовок: значение поля и выбор самого поля | HeadingOptions.jsx:135 | есть | `Heading` + `onHeading`; правится тем же кеглем, каким показан |
| Секции и порядок полей перетаскиванием | DrawerFormDetailPage.jsx:66 | есть | у нас тянуть может тот, у кого есть право на настройки; в старом — только роль с именем «DEFAULT ADMIN», зашитым строкой |
| Вкладки связей отфильтрованы по праву `view_permission` | index.jsx:147 | есть | те же вкладки, тот же отбор |
| Поле, скрытое из карточки (`field_hide_layout`) | index.jsx:163 | есть | см. раздел «Раскладка карточки» |
| Мультиязычные поля: переключатель языка сверху | DrawerFormDetailPage.jsx:225 | есть | вкладки языков; подпись идёт за выбранной вкладкой (`tabLabel`), а в таблице и фильтрах — за локалью интерфейса |
| «Только чтение» не действует при ЗАВЕДЕНИИ записи | DrawerFormDetailPage.jsx:362 (`&& isEditing`) | есть | добавлено этим проходом. Настройка про правку, а не про заполнение: иначе «Номер договора», закрытый от правки, нельзя было бы задать вообще нигде |
| Право роли на поле тоже снимается при заведении | там же | выброшено | это дыра: роль, которой поле править нельзя, заполняла бы его на новой записи. У нас `Field.locked` действует всегда — в отличие от `disabled` |
| Действия над записью внизу карточки | FormCustomActionButtons.jsx | есть | `TableActions` над открытой строкой — тот же компонент, что над выделением в таблице; рядом печатная форма |
| Сохранение записи заодно перезаписывает раскладку | index.jsx:224 (`updateLayout`) | выброшено | правка раскладки и правка записи — разные действия с разной ценой. У нас раскладка уезжает тогда, когда её меняли: перетащили поле, сменили заголовок |
| Особая проверка обязательных MULTI_LINE при создании | index.jsx:246 | есть | общая проверка строки: пробелы не заполняют обязательное поле у ЛЮБОГО текстового типа, а не только у MULTI_LINE. Причина одна и та же, а в старой админке ветка написана руками и под один тип |

## Редакторы значений и поле связи

Где смотреть: `views/views/components/DrawerDetailPage/ElementGenerator/`
— `DrawerFieldGenerator.jsx` (ветвление по типу), `RelationField.jsx`
(связь), `hf-*.jsx` (редакторы). Не референс —
`components/FormElements*` и `components/ElementGenerators/`
(предыдущее поколение тех же полей).

| Поведение | Где в старом | Статус | Чем закрыто |
|---|---|---|---|
| Свой редактор на каждый тип поля | DrawerFieldGenerator.jsx:128–467 | есть | у старого 29 ветвей, у нас 50 типов сведены к 26 видам редактора — включая JSON, QR, штрихкод, сканер, код и подпись-разделитель, которых в старом ветвлении нет вовсе |
| Три вида дат: дата, дата-время, дата-время без пояса | hf-datePickers.jsx:35, :115, :190 | есть | те же три (`date`, `datetime`, `datetime_naive`): без третьего значение уезжало бы в UTC и возвращалось на день назад |
| Телефон с выбором страны и форматированием | hf-internationalPhone.jsx:49 | частично | клавиатура под тип (`inputMode="tel"`), но без флагов и маски. В колонке всё равно лежит строка, а маска — ещё одно место, где введённое расходится с сохранённым; библиотека со списком стран ради этого не ставится |
| Видео: загрузка, просмотр, скачивание | hf-videoUploadField.jsx | есть | общий просмотрщик файлов: он же играет видео и звук, а не только показывает картинки |
| Пароль отдельным окном, а не ячейкой | DrawerFieldGenerator.jsx:227 | есть | `PasswordDialog` с подтверждением |
| Связь: поиск, догрузка страницами, автофильтр | RelationField.jsx:319 | есть | `useRelationRows` + `autoFilterValues` |
| «Открыть связанную запись» стрелкой на выбранном значении | RelationField.jsx:693 | сделано | стрелка рядом с выбранным значением открывает карточку чужой записи поверх всего (`item/ui/ForeignRecord`). Своего адреса у неё нет намеренно: у чужой таблицы может не быть пункта меню, а без него холодная ссылка осталась бы без раскладки и без прав. Только чтение: правка ушла бы в таблицу, которой на экране нет |
| Варианты связи считает ФУНКЦИЯ (`function_path`) | RelationField.jsx:265, Relations/FunctionPath.jsx | выброшено | мёртвая настройка: колонку `view.function_path` не пишет НИ ОДИН запрос — во всём объектном билдере она только читается (SELECT в `view.go:234`, `:428`; ни в INSERT, ни в UPDATE её нет). То есть выпадающий список «Function» в старой форме связи не сохраняет ничего, и ветка чтения не срабатывает никогда |
| Обязательное поле: текст ошибки под редактором | RelationField.jsx:731 | есть | та же проверка, что и у ячейки таблицы (`cellError`), плюс текст админа, если он его задал |

## Печатные формы («Docs»)

Где смотреть: пункт «Docs» в поповере настроек view
(`views/views/components/HeaderFilter/components/ViewOptions/ViewOptions.jsx:373`)
ведёт на `/{menuId}/templates` — экран `views/DocumentTemplates/`
(~1000 строк). Не референс: `Documents/Components/Template/` — это
второе, HTML-поколение тех же шаблонов.

Суть фичи: к таблице прикладывают файл .docx с переменными вида
`{слаг}`, а потом печатают им КОНКРЕТНУЮ запись — бэкенд подставляет
значения строки и отдаёт PDF (`POST /v2/docx-template/convert/pdf`,
шлюз `docx_template.go:809`).

| Что | Где в старом | Статус | Как у нас |
|---|---|---|---|
| Список шаблонов таблицы | DocumentTemplates/index.jsx:56 | есть | `features/docs`, панель в настройках view рядом с настройками таблицы: шаблоны у таблицы общие, во всех её view одни и те же |
| Завести шаблон | — (заводился редактором) | есть | загрузка готового .docx тем же загрузчиком, что и файл в ячейку; имя берётся из имени файла |
| Удалить шаблон | index.jsx:237 | есть | с подтверждением; сам файл в хранилище остаётся — его удаление ручкой не предусмотрено |
| Подсказка по переменным | components/Variables/index.jsx:40 | есть | список полей, щелчок кладёт `{слаг}` в буфер. Читаем из своей схемы: ручка `/v2/docx-template/fields/list` отдаёт те же поля и всегда пустой список связей (`docx.go:514`, ключ там назван «relations:» — с двоеточием) |
| Печать записи в PDF | index.jsx:101 (`onPDFDownloadClick`) | есть, и ближе к данным | кнопка в карточке записи: строка уже открыта, и заполнять форму заново не нужно. В старом это отдельный экран, куда запись передаётся параметром `?id=`. Тело запроса то же (`link` в строке запроса, `{table_slug, data}` в теле), но данные просеиваются: ключ с «_id» уезжает только если это связь LOOKUP с uuid — иначе шлюз идёт за строкой в несуществующую таблицу и отвечает 500 на весь документ (`features/docs/model/print-data`, [backend-notes](backend-notes.md)) |
| Открыть готовый PDF | index.jsx:85 (скачиванием) | есть | новой вкладкой — печатную форму сначала смотрят; если браузер её заблокировал (документ собирался дольше, чем живёт «пользовательское действие»), падаем на скачивание, как в старом |
| Переименовать шаблон | — (правился в редакторе) | пропущено | `PUT /v2/docx-template` живой, но формы переименования у нас нет: шаблон перезаливают файлом |
| Печать из карточки СВЯЗАННОЙ записи | — | пропущено | кнопка стоит в карточке записи самой таблицы; во вкладке связи карточка открывается без панели действий |
| Встроенный редактор .docx | components/Editor/index.jsx | выброшено | это ONLYOFFICE Document Server — отдельная служба, которой браузер сам подписывает JWT секретом `my_jwt_secret`, лежащим в исходнике старой админки. Шаблон делают в Word и загружают готовым |
| Просмотрщик PDF внутри админки | components/Viewer/index.jsx | выброшено | `@react-pdf-viewer` + свой воркер pdf.js ради предпросмотра; PDF открывается вкладкой браузера, у которого просмотрщик уже есть |
| Форма записи на экране шаблонов | components/ObjectForm/ | выброшено | нужна была потому, что печать жила отдельно от данных: экран заново собирал секции раскладки и значения строки. Печать из карточки делает это лишним |
| HTML-шаблоны (`/v1/html-template`) | services/documentTemplateService.js | сделано | второе поколение той же фичи, живёт параллельно docx — и стоит рядом с ним в панели «Печатные формы»: список, правка разметки, печать общим меню. Редактора с кнопками нет намеренно, разметка правится исходником |
| Экспорт HTML/PDF из HTML-шаблона | documentTemplateService.js:9 | частично | старая админка зовёт `/v2/utils/export/{slug}/html-to-pdf`, которого нет; работающие конвертеры лежат на `/v1/html-to-pdf` и `/v1/template-to-html`, и зовём мы их. Граница другая: у postgres-проекта ветка пуста (`view.go:97` — `// Does Not Implemented`) и ответ уходит `201` с пустой ссылкой. Считаем это отказом и говорим о нём вслух |

## Настройки (Settings)

Референс: `src/views/SettingsPopup/` — `SettingsPopup.jsx`,
`useSettingsPopupProps.jsx`, `modules/*`, `client-types/`,
`components/FolderCreateModal`. Не референс — `views/Objects/`
и всё, что открывается из настроек, но настройками не является
(конструктор таблиц, экран функций).

Разделов в старом окне тринадцать, и половина из них — не настройки,
а отдельные приложения внутри модалки (функции, микрофронтенды, файлы,
список таблиц). Здесь остались те, что настраивают проект и доступ
к нему; остальное разложено по строкам с причинами.

Ресурсы разделены надвое: «чем проект пользуется» (учётки провайдеров,
репозитории, переменные) — это настройка, и она здесь; «на чём проект
работает» (завести кластер, назначить его окружению) — это провижининг,
и его тут нет.

| Поведение | Где в старом | Статус | Чем закрыто |
|---|---|---|---|
| Окно с разделами слева, а не страница | SettingsPopup.jsx | есть | `SettingsDialog`; разделы сгруппированы («Аккаунт», «Проект», «Разработка») — одиннадцать пунктов подряд читаются списком, а не структурой |
| Раздел скрыт без глобального права роли | useSettingsPopupProps.jsx:286 (`settings_button`) | есть | `useGlobalRight` на разделе: `project_settings_button`, `environments_button`, `api_keys_button`, `redirects_button`, `version_button` — те же имена, что в матрице прав |
| `settings_button` убирает настройки ПРОЕКТА целиком | useSettingsPopupProps.jsx:286 (`tabs.splice(1)`) | есть | право стоит на группе, а не на разделе: у половины разделов своего права нет вовсе, и без группового они оставались бы видны роли, которой настройки проекта запрещены. Остаётся личная группа — профиль |
| Скрытый раздел закрыт и на сервере | — | заблокировано | ресурсы, печатные формы и логи функций висят под middleware, который прав роли не проверяет ВООБЩЕ ([backend-notes](backend-notes.md), «Права и middleware»). Наши кнопки — единственное, что стоит на пути |
| Профиль: имя, логин, почта, телефон, фото | modules/Account | есть | `ProfileSettings` |
| Смена пароля и открытые сессии | modules/Account | есть | там же, отдельными формами |
| Проект: имя, логотип, языки данных, пояс, валюта, наборы значков | modules/ProjectSettings | есть | `ProjectSettings` |
| Окружения: список, создание, правка, удаление | modules/Environment, EnvironmentDetail | есть | `EnvironmentSettings`; список берётся у переключателя в шапке (`features/workspace`) — второго запроса за тем же адресом нет |
| Цвет окружения пипеткой (`display_color`) | EnvironmentDetail.jsx:58 | частично | палитра из девяти оттенков вместо произвольного HEX: цвет уезжает в данные HEX'ом, но подобранный под светлую тему в тёмной становится пятном (та же причина, что у вариантов поля) |
| Удаление текущего окружения | Environment.jsx | выброшено | кнопка выключена: человек сидит в этом окружении, и удалить его — это остаться в сессии, которой некуда смотреть |
| Пользователи: вкладки по типам клиентов, список, страницы | client-types/index.jsx | есть | `UserSettings`; вкладки не украшение — общего списка «все люди проекта» в ручках нет, ответ приходит по одному типу |
| Заведение человека вручную: логин, пароль, роль | components/InviteModal | есть | форма в том же разделе; роли отбираются по типу клиента вкладки |
| Приглашение ссылкой | InviteModal.jsx:165 | есть | `model/invite` + диалог со ссылкой; писем бэкенд не шлёт вовсе — см. [backend-notes](backend-notes.md) |
| Правка и удаление человека | client-types/index.jsx | есть | те же поля, `PUT /v2/user` поверх ответа; удаление требует тип клиента параметром |
| Поля таблицы входа в форме человека (`client_type.columns`) | InviteModal.jsx:560 (`TypesComponent`) | выброшено | правка их не сохраняет: `PUT /v2/user` принимает двадцать полей proto и `additional_data` среди них нет (`user_service.pb.go:1793`). В старой админке не сохраняет и заведение — она шлёт поля верхним уровнем, где `ShouldBindJSON` их отбрасывает. Логин, пароль, роль и контакты — на месте |
| Права ролей: таблицы, поля, глобальные права, пункты меню | modules/PermissionsRoleDetail | есть | `RoleSettings` — сделано раньше этого прохода |
| Свои права: дерево, заведение, удаление, раздача роли | Permissions/CustomRow.jsx, AddCustomPermissionModal | есть | вкладка «Свои права» в `RoleSettings`: то же дерево по уровням, что и у меню, плюс «+» на строке для вложенного права. Право принадлежит ТИПУ КЛИЕНТА и раздаётся сразу всем его ролям — так его заводит бэкенд (`custom_permission.go:96`) |
| Правки своих прав уезжают по кнопке, а не по щелчку | CustomRow.jsx:97 | частично | ручка правит одну строку доступа за раз, списка она не принимает: копим и шлём пачкой запросов по «Сохранить» — чтобы слово «сохранено» значило на всех вкладках одно и то же |
| Свои права на mongo-проекте | — | заблокировано | шлюз отвечает «resource type not supported» всем, кроме PostgreSQL (`custom_permission.go:242`). Вкладка есть, список приедет пустым — чинить это на фронте нечем |
| Таблицы аудитории (`connections`): список, заведение, правка, удаление | PermissionsRoleDetail/ConnectionsModal, Matrix/ConnectionCreateModal | есть | кнопка «Таблицы» в строке типа клиента. Это меню приложения ЗАКАЗЧИКА: список уезжает ему вместе с ответом на вход (`login.go:190`) |
| Поля связки: имя, таблица, поле таблицы входа | ConnectionCreateModal.jsx:225–330 | частично | три поля вместо четырёх и с правильными подписями. В старой форме «Table slug» — это имя, «Field slug» — это `view_slug`, а сам `field_slug` подставляется из связи чужих таблиц и стирается при первой же правке (форма не шлёт его в PUT) |
| «Главная таблица» связки (`main_table_slug`) | ConnectionCreateModal.jsx:243 | выброшено | колонку не читает НИКТО: ни вход, ни `/v2/get-connection-options`, ни один запрос в четырёх сервисах. Обязательное поле, которое ни на что не влияет |
| Вид связки (`view_slug`) | ConnectionCreateModal.jsx:298 | выброшено | записать его можно, а взять неоткуда: у таблицы `view` колонки `slug` нет вовсе (`000001_init_tables.up.sql:398`) — только `id` и `name`. Старая форма кладёт туда слаг ПОЛЯ выбранной таблицы, и это не вид |
| Подпись вида и значок связки | login.go:190 | выброшено | вход их отдаёт, а записать их нечем: в `CreateConnectionRequest` нет ни `view_label`, ни `icon`, ни `type` (`connection_v2.go:3`) |
| Типы клиентов: список, создание, правка, удаление | components/FolderCreateModal, SettingsPopup.jsx:146 | есть | `ClientTypeSettings` — свой раздел перед «Пользователями» и «Ролями»: на типе висят и роли, и люди, и API-ключи. В старой админке он прятался кнопкой «Add client type» под списком прав |
| Таблица входа типа: выбрать или завести | FolderCreateModal.jsx:184 | есть | список отбирает сам шлюз (`is_login_table=true`); пустой выбор — «завести новую», её делает бэкенд. Слаг у автосозданной он записывает с ошибкой при имени с пробелом — чиним вторым запросом ([backend-notes](backend-notes.md)) |
| Предел сессий, саморегистрация, адрес после входа | FolderCreateModal.jsx:174–202 | частично | «Восстановление пароля» есть только при создании, «Страница после входа» — только при правке: другую половину бэкенд молча теряет на своём пути (`self_recorder` в правке, отсутствие `default_page` в создании) |
| Стратегия подтверждения (`confirm_by`) | — | выброшено | по ней никто не принимает решений: в ответе входа она захардкожена нулём (`login.go:357`). Сохранённое значение мы возвращаем бэкенду нетронутым — иначе правка перезаписала бы его |
| Набор полей формы человека (`client_type.columns`) | FolderCreateModal.jsx:204 | выброшено | значения этих полей можно ЗАДАТЬ при заведении человека (`additional_data`), но нельзя изменить: в `UpdateUserRequest` такого поля нет вовсе. Настройка, у которой работает половина пути, — это половина настройки |
| API-ключи: список, создание, правка, удаление | modules/ApiKeys, ApiKeysDetail | есть | `ApiKeySettings`; секрет спрятан и копируется кнопкой |
| Ограничения ключа: запросов в секунду и в месяц | ApiKeysDetail.jsx | выброшено | ни одна ручка их не пишет: в INSERT колонок нет, UPDATE пишет четыре других ([backend-notes](backend-notes.md)). Поле ввода, которое молча ничего не сохраняет, мы не рисуем |
| Список выданных токенов ключа | apiKey.service.js (`/tokens`) | выброшено | ручка ищет по `client_id`, а его заполняет только внешняя выдача токенов; у ключей, заведённых из админки, он пуст, и вкладка всегда пустая |
| Свои эндпоинты: список, создание, правка, удаление | modules/Redirect, RedirectForm | есть | `EndpointSettings`; приставка `/x-api/` показана, но не правится — другие пути через подмену не проходят (`api.go:37`) |
| Порядок правил перетаскиванием | Redirect.jsx (react-smooth-dnd) | сделано | перетаскивание на родном HTML5 drag-n-drop, без второй библиотеки; тот же `PUT /re-order` со всем списком. Строку переносят НА место цели, а не меняют местами. Стрелки «выше/ниже» остались: до них добираются с клавиатуры, до перетаскивания — нет |
| Проверка подстановок в правиле | — | есть | наше добавление: имя `{…}`, которого нет в «откуда», шлюз оставляет в адресе как есть — запрос молча возвращает 404. Ловим до сохранения |
| Журнал изменений: список, фильтры, страницы | modules/ActivityLogs | есть | `ActivityLog`; отбор целиком на сервере |
| Отбор по типу действия выпадающим списком | ActivityFeedTable (40 значений в коде) | частично | поле поиска по подстроке: список действий растёт вместе с ручками бэкенда, а зашитый перечень устаревает молча — сервер и сам сравнивает через ILIKE |
| Запись целиком: было, стало, запрос, ответ | modules/ActivityLogsDetail | есть | окно записи; конверт `{"data": …}` снимается один раз в `model/activity` |
| Логи функций (вторая вкладка журнала) | ActivityLogs/FunctionLogs | сделано | другая ручка (`/v2/functions/log`) и другой предмет — выполнение функции, а не изменение проекта; вкладка рядом с журналом изменений. Раскрытой записи нет: `FunctionLogModel` не несёт ни тела запроса, ни ответа. Единицы биллинга (`compute`, `*_bandwidth`) не показаны — раздела биллинга у нас нет. Ветка есть только у PostgreSQL (`function_logs.go:81`) |
| Выгрузка журнала в Excel | environmentService.js (`/history/{env}/excel`) | есть | кнопка в шапке журнала; выгружается ОТОБРАННОЕ, а не страница и не всё подряд — иначе файл выглядит как потеря фильтра. Бэкенд кладёт файл в хранилище и отвечает ссылкой |
| Внешние базы: подключение и выбор таблиц | modules/Models (вкладка ExternalDatabases) | есть | `ConnectionSettings`: подключение строкой соединения, отметка таблиц пачкой, снятие по одной |
| «Модели» — список таблиц проекта | modules/Models | выброшено | это список таблиц, а не настройка; им служит дерево пунктов, а настройки таблицы открываются там, где на неё смотрят (см. раздел Constructor) |
| Ресурсы проекта: список, создание, правка, удаление | modules/Resources, ResourcesDetail | есть | `ui/resources/` — раздел, выбор типа, форма, поля типа, переменные пятью файлами вместо 3.7 тыс. строк в одном каталоге. Поля типа — данные (`RESOURCE_SPECS`), а не своя форма на каждый: в старой админке пять форм отличались только подписями |
| Список из трёх источников: v1 + clickhouse + v2 | useResourcesProps.jsx:83 | исправлено | ответ v2 УЖЕ содержит базы проекта — это UNION с таблицей `resource` ([backend-notes](backend-notes.md)). Старая админка грузила их вторым запросом и показывала каждую дважды |
| SMS, SMTP, Mailchimp: учётка провайдера и настройка кода | ResourceContents/SmsContent, SmtpResource | есть | те же поля; «Длина кода» и «Универсальный код» подписаны тем, что они делают, — второй подходит всегда и провайдера не спрашивает (`session_service_v2.go:1110`) |
| Предупреждение о двух отправителях одного типа | — | есть | наше добавление: код берётся из ПЕРВОГО ресурса типа, а `ORDER BY` в запросе нет вовсе — при двух SMS-ресурсах неизвестно даже, какой из них сейчас работает |
| GitHub и GitLab через OAuth в новой вкладке | NewResourceDetail.jsx:385 | сделано иначе | блок «Подключённые аккаунты» под списком ресурсов: GitHub, GitLab и Bitbucket через СЕРВЕРНЫЙ OAuth (`/v1/<провайдер>/connect`), токен на фронт не приезжает вовсе. Это ДРУГАЯ сущность, чем ресурс GITHUB: `integration_resource` в company_service против `project_resource`, и читают их разные потребители. Старая админка их смешивает — собирает адрес авторизации прямо в браузере из `VITE_GITHUB_CLIENT_ID` (`ResourcesDetail/Form.jsx:60`), а токен всё равно оказывается в поле ресурса. Логин с токеном руками в ресурсе остаётся: он про другое |
| Superset и Metabase: адрес, логин, пароль | SupersetContent, MetabaseContent | частично | только чтение — как и в старой админке (поля `disabled`), и по той же причине: присланные настройки бэкенд перезаписывает своими (`resource.go:2546`) |
| Список дашбордов Metabase с копированием ссылки | MetabaseContent.jsx:123–200 | пропущено | это витрина аналитики, а не настройка ресурса. Ручки живы (`/v1/metabase/dashboard`, `/public-url`) — вернёмся отдельным экраном, если понадобится |
| Transcoder | TransCoder.jsx | частично | заводится именем, как и должен: 377 строк старой формы настраивают сам перекодировщик, а не ресурс |
| REST-ресурс и его переменные | Form.jsx:231, VariableResource.jsx | есть | пары «имя-значение» в той же форме. Создание кладёт их в тело ресурса, правка шлёт список (`PUT /resource-variable`) плюс DELETE на убранные: третьей ручки нет |
| Реконнект базы | ResourcesDetail.jsx:614 | есть | кнопка в карточке базы. Ответ читается по службам, и «не ответила» — это отсутствие `status`, а не `false` ([backend-notes](backend-notes.md)); старая админка ответ не читала и писала «успешно» всегда |
| Базы проекта (mongo, postgres, clickhouse) в списке | Resources.jsx | частично | видны и открываются на чтение — адрес, порт, пользователь, база. Правка и удаление скрыты: эта ручка их не удалит (`ErrNoRows` → 500) и не изменит (0 строк, ответ «успешно») |
| Провижининг: завести mongo, clickhouse, postgres | PostgresCreate.jsx, ClickHouseForm.jsx | сделано | группа «Базы» в выборе типа. PostgreSQL — подключение своей: адрес, порт, база, пользователь, пароль, имя подключения (`PostgresCreate.jsx` это форма реквизитов, а не провижининг). ClickHouse — заведение на платформе через `create-resource`, спрашивается только имя: реквизиты придумывает бэкенд. Форму реквизитов ClickHouse не переносим — в старой админке она собирает значения, а ветка отправки строит тело заново и введённого не читает (`Resources/Detail/index.jsx:337`). Mongo не заводим: старая админка этого тоже не делает |
| Ресурс POSTGRESQL как внешняя база | PostgresCreate.jsx | выброшено | это та же ручка объектного билдера, что и у «Внешних баз» (`resource.go:2582` против `table.go:1333`), но без второго шага — выбора таблиц, без которого подключение бесполезно. Одна дверь, и она уже есть |
| Окружения ресурса, «по умолчанию», конфигурирование | ResourceEnvironment.jsx, `/configure-resource`, `/resource-default` | частично | список окружений с отметкой «подключено» есть в карточке ресурса — ровно как в старой админке, где он тоже только показывает (обработчик клика закомментирован). Двух других переносить нечего: `configure-resource` недостижим — ветка стоит после `else if (!isEditPage)` внутри `else` от `if (isEditPage)`, а `resource-default` не зовётся ниоткуда вовсе |
| Google Drive, Calendar, Telegram, Instagram, Meta и Google Leads | — | частично | видны в списке, но не заводятся, а Telegram с Instagram и не удаляются: шлюз отвечает 400 со ссылкой на свою ручку подключения (`project_resource.go:56`). Раз ручка откажет — кнопки нет |
| Allowlisting: IP-адреса для доступа к чужой базе | AllowList.jsx | выброшено | четыре захардкоженных «21.651.6513651.51» — это заготовка, а не фича |
| Вкладка «SMS Template» у SMS-ресурса | SMSType.jsx:168 | выброшено | внутри неё обычная таблица проекта (`sms_template`) целиком, со своим view и формой. Таблица открывается пунктом меню — второе место для того же не нужно |
| Функции: список, создание, редактор кода, деплой | modules/Functions, FunctionsDetail | частично | раздел «Функции» в настройках: список, заведение, правка, удаление, запуск с ответом и ПРОСМОТР исходников. Редактора кода нет и не будет: ручки, которая пишет файлы функции обратно, в сервисе не существует — `push-changes` есть только у микрофронтенда. Путь и тип задаются один раз: путь — адрес развёрнутой функции и имя её репозитория. Запуск идёт через `/v2/functions/:id/run`, а не `/v1/invoke_function`: только первая отдаёт ответ функции наружу |
| Микрофронтенды: список, версии, промоут, откат | modules/MicroFrontend, MicroFrontendDetail | частично | раздел в настройках, [PLAN-v4](PLAN-v4.md) §2.3. Сделано всё, что пропускает шлюз: список, заведение, удаление, версии, снимок файлов версии, откат, публикация с опросом пайплайна. Не сделано и почему: правка записи (`PUT` на PostgreSQL уходит пустой структурой, на MongoDB стирает адрес сборки), редактор файлов (`files` и `push-changes` есть в сервисе, но шлюз их не выводит). Публикация показывается только у микрофронтендов из генератора: без `mcp_project_id` ручка отвечает 400 |
| Файлы проекта (Minio) | Files/, MinioPage | выброшено | у хранилища свой экран (`features/files`), и открывается он пунктом меню: папка — это и есть пункт. Второе место для того же — два списка одного хранилища |
| Language Control: перевод подписей самой админки по проекту | LayoutSidebar/Components/LanguageControl | выброшено | интерфейс переводим мы, а не проект: у нас три локали в `src/locales`, типизированные по en.json. Проектные переводы кнопок — это то, из-за чего в старой админке один и тот же пункт назывался по-разному в двух местах |
| Биллинг и тарифы | modules/Billing, Fares | выброшено | биллинга в проекте нет — то же решение, что и по баннеру подписки (см. раздел Sidebar). Не биллинг, а лимиты: расход API-запросов показывается вкладкой Usage в журнале (`features/settings/ui/Usage.tsx`) — это потолок, в который проект упирается, а не деньги |
