/**
 * Системные пункты меню, которые бэкенд заводит в каждом проекте
 * (ucode_go_object_builder_service/config/constants.go, STATIC_MENU_IDS).
 *
 * В сайдбаре они не показываются: это не содержимое проекта, а входы
 * в отдельные экраны. Старая версия прятала ровно эти три, но списком
 * голых UUID внутри фильтра — здесь у каждого есть имя и адрес.
 */
export const SYSTEM_MENUS = {
  /** Корень дерева. Не пункт, а контейнер: с него начинается загрузка. */
  ROOT: "c57eedc3-a954-4262-a0af-376c65b5a284",

  /** Настройки проекта. Экран: /settings, вход — из шапки сайдбара. */
  SETTINGS: "c57eedc3-a954-4262-a0af-376c65b5a280",

  /** Файловое хранилище. Экрана пока нет — появится отдельным разделом. */
  FILES: "8a6f913a-e3d4-4b73-9fc0-c942f343d0b9",

  /** Пользователи и роли. Уедет в настройки, к правам. */
  USERS: "9e988322-cffd-484c-9ed6-460d8701551b",
} as const;

/** Что именно скрываем из списка меню. Корень сюда не входит: он и так не пункт. */
const HIDDEN = new Set<string>([SYSTEM_MENUS.SETTINGS, SYSTEM_MENUS.FILES, SYSTEM_MENUS.USERS]);

export const isSystemMenu = (id: string) => HIDDEN.has(id);
